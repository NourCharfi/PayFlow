# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/produit_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Other configurations remain the same
spring.cloud.discovery.enabled=true
monParam=555
email=nassiim.farhat@gmail.com

# JWT Resource Server
# spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8190/realms/Pay-Realm
# Désactivation de la vérification de l'issuer pour accepter les tokens custom